package com.setu.app.data.remote

/**
 * Retrofit interface — matches backend/app/routers/records.py.
 * TODO: add Retrofit annotations (@POST etc.) once the dependency is added.
 */
interface ApiService {
    // @POST("records/batch")
    // suspend fun pushBatch(@Body batch: RecordBatchRequest): RecordBatchResponse
}
