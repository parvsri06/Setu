package com.setu.app.ui.status

import androidx.compose.runtime.Composable

/** Shows sync status, e.g. "synced with 2 nearby devices, last update 4 min ago". */
@Composable
fun SyncStatusScreen() {
    // TODO: wire to SyncManager's state once it's built.
}
