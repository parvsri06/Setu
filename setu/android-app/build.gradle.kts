// Top-level build file. Plugin versions to be pinned once the team starts
// actually building — left unpinned here since this is structure only.
plugins {
    id("com.android.application") version "8.5.0" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
