-- Initial migration — currently identical to schema.sql.
-- Once schema.sql has been applied once and the team starts changing it,
-- put each subsequent change in a new numbered file here instead of editing
-- schema.sql directly, so the history of changes is trackable.
\i ../schema.sql
